resources:
  - ../../base
namePrefix: test-

# 1. This updates the image tag
images:
  - name: quay.io/libpod/alpine
    newName: registry.access.redhat.com/ubi9/ubi-minimal
    newTag: "9.3"

# 2. This injects the "dev" identity into the pod
patches:
  - target:
      kind: Deployment
      name: my-app
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/env/0/value
        value: "test"
      - op: replace
        path: /spec/template/spec/containers/0/env/1/value
        value: "registry.access.redhat.com/ubi9/ubi-minimal:9.3"